// $(window).scroll(function() {
//         var scrollY = ($(window).scrollTop() / ($(document).height() - $(window).height()) * 100).toFixed(3);
//         $(".scrollbar_thumb").css({"top" : scrollY + "%"});
// });

// window.addEventListener('scroll', function() {
//     var scrollY = (window.pageYOffset / (document.documentElement.scrollHeight - window.innerHeight) * 100).toFixed(3);
//     var scrollbarThumb = document.querySelector('.scrollbar_thumb');
//     scrollbarThumb.style.top = scrollY + '%';
//   });
  

// window.addEventListener('load',()=>{
//     window.addEventListener('scroll', function() {
//         var scrollY = (window.pageYOffset / (document.documentElement.scrollHeight - window.innerHeight) * 100).toFixed(3);
//         var translateYValue = scrollY + '%';
        
//         var scrollbarThumb = document.querySelector('.scrollbar_thumb');
//         scrollbarThumb.style.transform = 'translateY(' + translateYValue + ')';
//       });
// })

